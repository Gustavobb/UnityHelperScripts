using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tester4 : MonoBehaviour
{
    // Start is called before the first frame update

    void Start()
    {
        // new QuietTimer(2f, 0f, this, ()=>{GameManager.levelManager.LoadNextScene();}).Execute();
        // new QuietTimer(2f, 0f, this, () => {GameManager.audioManager.PlayAudio("win", 1f, 1f);}).Execute();
        // new QuietTimer(2f, 0f, this, () => {GameManager.audioManager.PlayAudio("loose", 1f, 1f);}).Execute();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
